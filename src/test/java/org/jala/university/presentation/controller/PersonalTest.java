package org.jala.university.presentation.controller;

class PersonalTest {

/*
    @InjectMocks
    private ValidateCreditCard validateCreditCard;

    @BeforeEach
    void setUp() {

        MockitoAnnotations.openMocks(this);
    }

    @Test
    public void valid_credit_card() throws SQLException {
        Date date = new Date("2024/11/06");
        CreditCard creditCard = new CreditCard(1, "12345678", "123", date, true, "Rodrigo S", "12345678", new BigDecimal(450.43), "Rodrigo Santos");


        List<CreditCard> result = validateCreditCard.validateFilds(creditCard); // Tipo correto aqui


        assertNotNull(result);
        assertEquals(1, result.size());
        assertEquals("Rodrigo S", result.get(0).getName_card());
        assertEquals(1, result.get(0).getId_credit_card());
    }*/
}
